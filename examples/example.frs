< comments sometimes buggy - forgot do ignore string and brackets >
< memory leak on runtime - because of string concat >

var STRING = "";
var CHAR   = ' ';
var INT    = 0;
var FLOAT  = 0.0;

read (type = INT) {
    var x = type;
    I: &x;
    ret x;
}

print (x) > O: x, '\n';

sleep (x) > S: "sleep $x";

for (min, max, lambda) {
    (max - min + 1) -> {
        lambda (min);
        min += 1;
    }
}

< Example program >

for (read(), read(), (x) {
    O: ':', x, '\n';
});

