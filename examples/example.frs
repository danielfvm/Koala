<* default value does not work! *>
<* ret doesn't work in lambda *>


for (min, max, lambda = (x) { O: x, '\n'; }) {
    (min < max + 1) -> {
        lambda (min);
        min += 1;
    }
}

isPrime (x) {
    (x < 2) > ret false;

    var j = 2;
    (j < x) -> {
        !(x % j) > ret false;
        j += 1;
    }

    ret true;
}

for (0, 100, (i) {
    (isPrime (i)) > O: i, '\n';
});

