< re1t in if doesn't work! >
< default value does not work! >
< comments not working properly >

isPrime (x) {
    var j = 2;
    var prime = true;
    (j < x) -> {
        !(x % j) > prime = false;
        j += 1;
    }
    ret prime;
}

var i = 0;

(i < 100) -> {
    (isPrime (i)) > O: i, '\n';
    i += 1;
}
