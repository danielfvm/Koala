< lambda still not working perfect >
< comments sometimes buggy >

var STRING = "";
var CHAR   = ' ';
var INT    = 0;
var FLOAT  = 0.0;

read (type = INT) {
    var x = type;
    I: &x;
    ret x;
}

print (x) > O: x, '\n';

sleep (x) > S: "sleep $x";

for (min, max, lambda) {
    (max - min + 1) -> {
        lambda (min);
        min += 1;
    }
}




< Example program >

print ("Loop from-to:");

for (read(), read(), (i) { 
    O: ":$i\n";
});
